// src/app/sleep/layout.js

export const metadata = {
  title: 'Sleep Cycle Calculator | The Daily Scale',
  description: 'Discover the perfect time to wake up or fall asleep using 90-minute sleep cycles. Avoid grogginess and wake up feeling refreshed.',
  keywords: 'sleep cycle calculator, rem sleep calculator, best time to wake up, 90 minute sleep cycle',
};

export default function SleepLayout({ children }) {
  return <>{children}</>;
}